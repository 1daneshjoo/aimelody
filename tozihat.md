سند جامع نیازمندی‌های پلتفرم AiMelody.ir (نسخه ۲)
۱. ساختار کلی و انواع محتوا (Core Content)
پشتیبانی از دو نوع محتوای مجزا با رابط کاربری (UI) متفاوت:
فایل صوتی (Audio): ظاهر شبیه اسپاتیفای (کاور مربع، پلیر صوتی با موج صدا / Waveform، نمایش متن ترانه).
فایل ویدئویی (Video): ظاهر شبیه یوتیوب (کادر مستطیل ۱۶:۹، پلیر اختصاصی ویدئو).
۲. سیستم کاربری و احراز هویت (User & Auth System)
ثبت‌نام و ورود پیامکی (OTP): ورود و عضویت فقط با شماره موبایل.
اتصال به API پیامکی: استفاده از مستندات سامانه IPPanel (ippanelcom.github.io) برای ارسال سریع کد تایید پیامکی (Pattern-based SMS).
سطوح کاربری: مهمان (فقط مشاهده)، کاربر تاییدشده (رای‌دهی و آپلود)، کاربر VIP (دسترسی ویژه)، مدیر سایت.
۳. سیستم ارسال و آپلود اثر توسط سازنده (Ajax Upload System)
فرم آپلود آجاکس (Ajax): ارسال اطلاعات و فایل بدون رفرش شدن صفحه همراه با نوار پیشرفت (Progress bar).
فیلدهای اجباری و اختیاری فرم آپلود برای سازنده:
عنوان اثر (اجباری).
نوع اثر (انتخاب بین صوتی یا ویدئویی).
آپلود فایل اصلی (یا قرار دادن لینک مستقیم فایل).
آپلود عکس کاور (مخصوص فایل‌های صوتی).
ژانر/سبک موسیقی (پاپ، رپ، سنتی، راک و...).
متن ترانه / Lyrics (اختیاری).
نام ابزارهای هوش مصنوعی استفاده شده (مثلاً: Suno برای صوت، Runway برای ویدئو).
پرامپت (دستور متنی) استفاده شده (اختیاری برای کاربر / قابل تنظیم به عنوان محتوای VIP).
توضیحات سازنده (اختیاری).
۴. سیستم رتبه‌دهی چندبُعدی (Multi-Parameter Rating)
محدودیت دسترسی: ثبت رای فقط برای کاربران لاگین‌شده با شماره موبایل (جهت جلوگیری از تقلب).
پارامترهای امتیازدهی (از ۱ تا ۵ ستاره یا نمره ۱ تا ۱۰):
کیفیت شعر و ترانه.
کیفیت ملودی و آهنگسازی.
طبیعی بودن صدای خواننده (Vocals).
کیفیت بصری (فقط برای موزیک‌ویدئوها).
امتیاز کلی (Overall Rating): یک فیلد مجزا که کاربر احساس نهایی و کلی خود را به اثر می‌دهد (مستقل از ریزامتیازات بالا).
محاسبه خودکار میانگین هر پارامتر و نمایش نمره کل در صفحه اثر.
۵. چارت‌ها و مرتب‌سازی پیشرفته (Leaderboards & Filters)
فیلترهای زمانی: برترین‌های روز، هفته، ماه و تمام دوران.
فیلترهای تخصصی: مرتب‌سازی بر اساس بهترین ترانه، طبیعی‌ترین صدا، بالاترین امتیاز کلی و پرطرفدارترین موزیک ویدئوها.
۶. سیستم مسابقات و چالش‌ها (Competitions System)
تعریف چالش‌ها توسط مدیر (مثلاً: "بهترین آهنگ با شعر مشخص X").
اتصال فرم آپلود به مسابقات (کاربر هنگام آپلود مشخص می‌کند که اثر برای کدام مسابقه است).
رای‌گیری زمان‌دار مردمی برای تعیین برنده.
۷. سیستم درآمدزایی و تبلیغات (Monetization & Ads)
اسپانسرینگ ویدئوها: درج لوگو، بنر یا پخش تیزر کوتاه (Pre-roll / Mid-roll) در ویدئوها.
آهنگ‌های پروموت شده (Promoted): سنجاق کردن آهنگ‌هایی که هزینه پرداخت کرده‌اند در بالای چارت‌ها.
تبلیغات کلیکی/بنری در سایدبار و بین لیست‌ها.
فروش اشتراک VIP برای دسترسی به پرامپت‌های حرفه‌ای.
۸. ساختار صفحات سایت (Site Pages Structure)
صفحه اصلی (Home): نمایش ویترین سایت، چارت‌های اصلی، مسابقات فعال و آثار پروموت شده.
صفحه جزئیات اثر (Single Page): شامل پلیر (صوتی/تصویری)، اطلاعات اثر، تب متن ترانه، فرم امتیازدهی ۵ گانه، و بخش کامنت‌ها.
صفحه آرشیو و جستجو (Explore/Charts): دایرکتوری کامل آهنگ‌ها با قابلیت فیلتر پیشرفته.
صفحه آپلود آجاکس (Upload Page): فرم چندمرحله‌ای و روان برای ارسال اثر.
داشبورد کاربری (User Panel): پنل اختصاصی هر کاربر شامل: ویرایش پروفایل، لیست آثار ارسال‌شده‌ی خود فرد، وضعیت تایید/رد آثار توسط مدیر، و لیست علاقه‌مندی‌ها (Favorites).
پنل مدیریت (Admin Panel): محیط بک‌اند برای تایید/رد آثار ارسالی کاربران، مدیریت مسابقات، مدیریت تبلیغات و کاربران.
صفحات پایه: مسابقات (لیست چالش‌ها)، درباره ما، قوانین و مقررات، خرید اشتراک.

---

## پیشنهاد استک تکنولوژی (Tech Stack Recommendation)

### جمع‌بندی پیشنهادی

| لایه | پیشنهاد اصلی | جایگزین قابل قبول |
|------|--------------|-------------------|
| فرانت‌اند | **Next.js (App Router) + TypeScript + Tailwind CSS** | Nuxt 3 (Vue) |
| بک‌اند | **NestJS + TypeScript** | Laravel (PHP) |
| دیتابیس | **MySQL 8** (انتخاب نهایی) | PostgreSQL |
| ORM / دسترسی داده | **بدون Prisma** — Query Builder / SQL خام یا TypeORM/Drizzle/Eloquent | Prisma (رد شد) |
| ذخیره فایل‌ها | Object Storage (S3-compatible مثل MinIO / لیارا / آروان) | دیسک سرور (فقط برای شروع) |
| احراز هویت | JWT + OTP پیامکی (IPPanel) | Session-based (در Laravel ساده‌تر است) |
| UI RTL / فارسی | Tailwind + فونت فارسی (مثلاً Vazirmatn) | — |

---

### چرا این ترکیب؟

#### فرانت‌اند: Next.js + TypeScript + Tailwind

- پلتفرم محتوایی است (چارت، اثر، مسابقه، SEO) → SSR/SSG در Next.js برای ایندکس گوگل مهم است.
- دو UI متفاوت صوت (اسپاتیفای‌مانند) و ویدئو (یوتیوب‌مانند) با React و اکوسیستم پلیرها راحت‌تر پیاده می‌شود.
- با **mock data** شروع می‌کنیم؛ بعداً فقط لایه API عوض می‌شود و صفحات دست‌نخورده می‌مانند.
- TypeScript از اول قرارداد داده (اثر، رای، کاربر، مسابقه) را ثابت نگه می‌دارد و اتصال به بک‌اند را کم‌ریسک می‌کند.
- Tailwind برای RTL فارسی و طراحی سریع صفحات مناسب است.

#### بک‌اند: NestJS + TypeScript (پیشنهاد اول)

- یک زبان در فرانت و بک → تایپ‌های مشترک، توسعه و نگهداری ساده‌تر.
- ماژولار است: Auth/OTP، Upload، Rating، Charts، Competitions، Ads، Admin جدا و تمیز.
- آپلود با progress، صف پردازش فایل، و APIهای فیلتر/چارت را خوب پوشش می‌دهد.
- اتصال به IPPanel از طریق HTTP client استاندارد ساده است.

#### جایگزین بک‌اند: Laravel (اگر هاستینگ ایران اولویت باشد)

- روی هاست‌های اشتراکی/مدیریت‌شده ایران معمولاً آماده‌تر و ارزان‌تر است.
- پنل ادمین با Filament خیلی سریع بالا می‌آید (تایید اثر، مسابقه، تبلیغات، کاربران).
- Queue برای SMS و پردازش فایل بالغ است.
- اگر تیم یا سرور بیشتر PHPمحور است، Laravel انتخاب عملی‌تری است.

---

### دیتابیس و فایل

- فایل‌های صوت/ویدئو/کاور را روی دیسک اپ نگذارید؛ از **Object Storage** استفاده شود (لینک مستقیم فایل در فرم آپلود هم با همین مدل سازگار است).

#### چرا PostgreSQL پیشنهاد شد؟

برای AiMelody این کارها زیاد تکرار می‌شود:

- میانگین چند پارامتر امتیاز (شعر، ملودی، وکال، بصری، overall)
- چارت‌های فیلترشده (روز / هفته / ماه / همه) با مرتب‌سازی روی فیلدهای مختلف
- یکتایی رأی (یک کاربر = یک رأی برای هر اثر)
- روابط چندجدولی: کاربر ↔ اثر ↔ رأی ↔ مسابقه ↔ تبلیغ

PostgreSQL در این سناریوها معمولاً راحت‌تر و دقیق‌تر است:

- Aggregation و window functionهای قوی برای چارت و میانگین
- Constraint و ایندکس بالغ‌تر برای جلوگیری از رأی تکراری و دیتای خراب
- پشتیبانی خوب از JSON وقتی بعداً متادیتای انعطاف‌پذیر لازم شود (مثلاً تنظیمات تبلیغ / VIP)
- سازگاری عالی با Prisma و NestJS

برای این پروژه «باید حتماً Postgres باشد» نیست؛ بیشتر به‌خاطر گزارش‌گیری و امتیازدهی چندبُعدی پیشنهاد اول بود.

#### نظر درباره MySQL 8

**MySQL 8 کاملاً قابل قبول و گزینه عملی خوبی است**، مخصوصاً اگر:

- هاست/سرور ایران MySQL را آماده‌تر یا ارزان‌تر بدهد
- با MySQL آشناتر هستید
- بک‌اند Laravel شود (اکوسیستم MySQL در ایران خیلی رایج است)

MySQL 8 برای نیازمندی‌های فعلی AiMelody کافی است:

- Window functions دارد (برای رتبه در چارت)
- CTE دارد
- JSON خوب پشتیبانی می‌کند
- InnoDB برای تراکنش رأی/آپلود قابل اعتماد است

تفاوت مهم در این پروژه معمولاً «امکان‌پذیری» نیست؛ بیشتر راحتی کوئری‌های تحلیلی و سلیقه تیم/هاست است.

| موضوع | PostgreSQL | MySQL 8 |
|------|------------|---------|
| چارت و میانگین امتیاز | عالی | خوب و کافی |
| یکپارچگی با Prisma | عالی | خوب |
| رایج بودن روی هاست ایران | متوسط | خیلی رایج |
| پیچیدگی ادمین سرور | کمی بیشتر | معمولاً ساده‌تر |
| آینده‌نگری گزارش‌های پیچیده | قوی‌تر | کافی برای نسخه ۱–۲ |

**جمع‌بندی دیتابیس:**  
اگر هاست/تسلط تیم خنثی است → PostgreSQL.  
اگر راحتی دیپلوی در ایران اولویت است → **MySQL 8 را انتخاب کن؛ اشتباه نیست.**

#### چرا Prisma؟

Prisma ORM است، دیتابیس نیست. نقش‌اش این است:

- اسکیما را با TypeScript هم‌تراز نگه می‌دارد (`schema.prisma` → تایپ‌های آماده)
- Migration تمیز می‌دهد (ساخت/تغییر جدول‌ها)
- جلوی خیلی از باگ‌های SQL دستی را می‌گیرد
- هم با PostgreSQL و هم با MySQL 8 کار می‌کند

یعنی انتخاب Prisma مستقل از Postgres/MySQL است.  
اگر MySQL 8 را انتخاب کنید، باز هم می‌توانید Prisma را نگه دارید.

جایگزین‌ها:

- **TypeORM / Drizzle** با NestJS
- **Eloquent** اگر بک‌اند Laravel شود

---

### فازبندی پیشنهادی توسعه

1. **فاز ۱ (فعلی):** فقط فرانت Next.js با داده‌های mock برای تمام صفحات.
2. **فاز ۲:** نهایی‌سازی UI/UX بر اساس بازخورد.
3. **فاز ۳:** بک‌اند + دیتابیس + OTP واقعی + آپلود واقعی.
4. **فاز ۴:** پنل ادمین، مسابقات، تبلیغات
   - بخش VIP از اسکوپ فعلی فرانت حذف شده است.

---

### چیزهایی که عمداً پیشنهاد نمی‌شود (برای این پروژه)

- **PHP خام / CodeIgniter قدیمی:** برای این سطح پیچیدگی نگهداری سخت می‌شود.
- **فقط HTML استاتیک بدون فریم‌ورک:** اتصال بعدی به API و state پلیر/آپلود دردسرساز است.
- **WordPress به‌عنوان هسته پلتفرم:** برای چارت، امتیاز چندبُعدی و آپلود سازنده مناسب نیست.
- **MongoDB به‌عنوان دیتابیس اصلی:** مدل رای/چارت رابطه‌ای است؛ MySQL 8 / PostgreSQL مناسب‌ترند.
- **Prisma:** برای این پروژه انتخاب نشد؛ دسترسی به MySQL 8 بعداً بدون Prisma انجام می‌شود.

---

### تصمیم نهایی قطعی

- **فرانت:** Next.js + TypeScript + Tailwind (با mock / دمو)
- **بک (بعداً):** NestJS (یا Laravel در صورت نیاز هاست)
- **دیتابیس:** MySQL 8
- **ORM:** بدون Prisma — بعداً با Query Builder / SQL یا TypeORM/Drizzle/Eloquent